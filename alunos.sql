CREATE TABLE ALUNOS (ID INT,  RA INT, NOME VARCHAR(512));

DELIMITER //

CREATE PROCEDURE POPULA_ALUNOS()
BEGIN
	DECLARE I INTEGER;
	DECLARE NOME VARCHAR(512);
	SET I=1;
	WHILE (I < 100000) DO
		SET NOME = CONCAT("ALUNO", I);
		INSERT INTO ALUNOS VALUES(I, RAND()*100000, NOME);
		SET I=I+1;
	END WHILE;

END//
DELIMITER;
